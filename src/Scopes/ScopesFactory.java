package Scopes;

import Variables.VariableException;

import java.util.ArrayList;

public class ScopesFactory {
    public Scope createScope(Scope parent, ArrayList<String> lines) throws VariableException {
        return createScope(parent, lines);
    }
}
